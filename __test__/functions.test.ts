import {
    getDoubleNumber,
    getNumber,
    getText,
    someFunction,
} from "../src/functions";

describe("Test", function () {
    it(``, async () => {
        expect(await someFunction(5)).toBe(25);
    });
});
